// require libs
import { AppRegistry } from 'react-native';
import App from './App/App';

// don't not change 'OneStopCommute' as this is required
AppRegistry.registerComponent('OneStopCommute', () => App);